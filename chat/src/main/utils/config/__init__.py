"""Application configuration helpers.

- ``loader``        - YAML config + secrets resolution (``resolved_config``,
                       ``resolved_secrets``, ``resolved_prompts``).
- ``defaults``      - Default user/system settings seeding.
- ``desktop_mode``  - Detection helpers for desktop vs. server runtime mode.
"""
