"""Core cross-cutting utilities.

Foundational modules that nearly every other package depends on:

- ``logger``       - Logging configuration and ``get_logger`` factory.
- ``decorators``   - Spring-style ``@service`` / ``@autowired`` DI decorators.
- ``error_codes``  - Mapping of internal status keys to user-facing codes.
"""
